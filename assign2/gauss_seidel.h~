#include <stdio.h>
#include <math.h>
#include <stdlib.h>

void gauss_seidel(double **OLD, double **NEW, int size, double TOL, int max_it, \
              double h);
void gauss_update(double **OLD, double **NEW, int size, double h);

